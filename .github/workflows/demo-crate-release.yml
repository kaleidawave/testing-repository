name: Demo crate release

on:
  push:
    paths:
      - "demo-crate-6154/**"
      - ".github/workflows/demo-crate-release.yml"

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install Rust 1.50
        uses: actions-rs/toolchain@v1
        with:
          toolchain: 1.50.0
          override: true
      - name: Run cargo check
        uses: actions-rs/cargo@v1
        with:
          command: check
      - name: Crates publish
        uses: kaleidawave/crates-release-gh-action@v1
        id: release
        with:
          version: minor
          crates-token: ${{ secrets.DEMO_CRATE_RELEASE_CRATES_TOKEN }}
          working-directory: demo-crate-6154
