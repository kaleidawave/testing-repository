name: Get video

on:
  push:
    paths:
      - "video/video-requests.txt"

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install & setup
        run: |
          brew install yt-dlp/taps/yt-dlp
          brew install ffmpeg
      - name: Download and trim
        run: |
          line=$(tail -n 1 videos/video-requests.txt)
          items=($line)
          yt-dlp ${items[0]} --output videos/output.mp4
          ffmpeg -i videos/output.mp4 -ss "${items[1]}:00" -to "${items[2]}:00" -c:v copy -c:a copy videos/output.mp4
      - name: Upload
        uses: actions/upload-artifact@v3
        with:
          name: videos
          path: |
            videos