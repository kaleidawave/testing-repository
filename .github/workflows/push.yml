name: Testing things

on:
  push:
    paths:
      - ".github/workflows/push.yml"
      - "one-off/**/*"

jobs:
  download-binaries:
    runs-on: ubuntu-latest

    env:
      GH_TOKEN: ${{ github.token }}

    steps:
    - uses: kaleidawave/release-downloader@main
      with:
        items: kaleidawave/experiements@assets[shell-language],kaleidawave/experiements@assets[spectra]
      
    - name: Test 1
      run: spectra

    - name: Test 2
      run: shell-language
