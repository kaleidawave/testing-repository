name: Testing things

on:
  push:
    paths:
      - ".github/workflows/push.yml"
      - "one-off/**/*"

jobs:
  start_discussion:
    runs-on: ubuntu-latest
    permissions:
      discussions: write  # Needed to create discussions
      contents: read

    steps:
      - uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const query = `mutation($repositoryId:ID!,$categoryId:ID!,$body:String!,$title:String!) {
              # input type: CreateDiscussionInput
              createDiscussion(input: {
                repositoryId: $repositoryId, 
                categoryId: $categoryId, 
                body: $body, 
                title: $title
              }) {

                # response type: CreateDiscussionPayload
                discussion {
                  id, url
                }
              }
            }`;

            const variables = {
              repositoryId: 'R_kgDOHITK2Q',
              categoryId: 'DIC_kwDOHITK2c4CTuRQ',
              title: 'Hello World',
              body: 'Discussion',
            }
            const result = await github.graphql(query, variables)
            console.log(result)
            
            core.info(`Discussion created: ${discussion.createDiscussion.discussion.url}`);
  